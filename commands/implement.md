---
description: Run the devloop implement stage — work a finished PLAN.md into code, tests, and commits.
argument-hint: "<feature-name>"
---

Use the `implement` skill to run the implement stage for the feature: **$ARGUMENTS**

The skill dispatches the `implementer` agent to work the PLAN.md tasks test-first and commit them. If no feature name was given above, ask for one before proceeding.
