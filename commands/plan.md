---
description: Run the devloop plan stage — turn a finished SPEC into a traceable PLAN.md task breakdown.
argument-hint: "<feature-name>"
---

Use the `plan` skill to run the plan stage for the feature: **$ARGUMENTS**

The skill dispatches the `planner` agent to write `specs/<slug>/PLAN.md`. If no feature name was given above, ask for one before proceeding.
