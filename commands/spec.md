---
description: Run the devloop spec stage — turn a feature into a falsifiable SPEC.md contract.
argument-hint: "<feature-name>"
---

Use the `spec` skill to run the spec stage for the feature: **$ARGUMENTS**

The skill produces the durable `specs/<slug>/SPEC.md` contract. If no feature name was given above, ask for one before proceeding.
